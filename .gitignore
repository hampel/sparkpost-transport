vendor/
composer.lock
composer.phar
CLAUDE.local.md
.idea/
.phpunit.cache/
.phpunit.result.cache
.php-cs-fixer.cache
.DS_Store

# environment files for harness exercises - added with harness/, not with the
# first credential, because the leak happens at the commit that adds the value
.env
.env.*
!.env.example
